This is a string calculator that can parse a formatted string containing numbers and calculate the sum. See https://github.com/restaurant365/challenge-calculator-js for requirements.

Bootstrapped with create-react-app

To start, run 'npm start'
To test, run 'npm run test'

Documentation is placed in the out folder, generated by JSDoc